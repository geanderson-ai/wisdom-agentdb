# Wisdom AgentDB CLI

Minimal command-line interface for the memory API.

## Commands

- Build first: `pnpm build:ts`
- `node dist/wisdom.js health`
- `node dist/wisdom.js add "<text>"`
- `node dist/wisdom.js search "<query>"`
