{
  "name": "@wisdom-agentdb/cli",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "wisdom": "./dist/wisdom.js"
  },
  "main": "./dist/wisdom.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "pnpm build && node ./dist/wisdom.js",
    "lint": "tsc -p tsconfig.json --noEmit",
    "test": "tsc -p tsconfig.json --noEmit"
  }
}
